# Z Smith Github Pages

My public website, which currently serves as an academic CV.

To build locally:
1) Install [Jekyll](https://jekyllrb.com/docs/installation/), which requires Ruby.
2) Run `gem install "jekyll-theme-cayman"`
3) Run `jekyll serve`
